[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sigdeck"
version = "2.1.0"
description = "Offline Ed25519 signing toolkit - pure-python RFC 8032, armor, QR exchange"
